# 工作日志 2026-08-24

## 群聊体验改造（接续 8-18 清单，A–H 全部完成）

- **退出确认弹窗**：新组件 `RoomLeaveConfirm.tsx`，群主红色「退出并解散」警告/成员普通确认；接入 RoomStage 顶栏与侧栏两处原本无确认的退出入口
- **邀请码弹窗**：新组件 `RoomInviteModal.tsx`，只读框 + 复制按钮（2s「已复制」反馈）+ 防火墙提示，去掉自动复制
- **加 Agent 席位弹窗**：新组件 `RoomAddSeatModal.tsx`，席位名 + 人设下拉（description 预览）
- **输入区重设计**：圆角容器 = 引用预览条 + textarea + 底栏（🎲✊✌️✋ + 发送）；@提及复用 `parseTrailingAt` + `.slash-menu` 弹层键盘导航，消息内 `@席位名` 高亮（@自己加底色，纯渲染层）；消息 hover「引用」→ `sendToSeat(text, quote)`，quote 块渲染
- **断线 UI**：侧栏房间行「已断线 + 重连」、RoomStage 断线横幅 + 重新连接按钮、输入区禁用
- **项目文件夹可选化**：sessionStart 无项目时回退 `lastProjectPath ?? os.homedir()`，不再抛错；Composer 解除未选文件夹禁用，输入区左下新增文件夹 chip
- **验证**：typecheck ✅；测试 349/349（新增 quote 透传单测）✅；build ✅。追踪文档已更新为完成态

## UI 打磨（多轮反馈）

- 邀请码弹窗日间模式全黑修复（`--bg-inset` 未定义落到黑色兜底 → 改 `--bg-input`）
- Composer 焦点边框改柔和 accent 光晕；上传按钮改圆形 `+`；文件夹 chip、模型选择统一为 30px 胶囊玻璃风
- textarea 随内容自动拉高（上限 180px）
- 权限模式下拉左移 + 菜单右对齐，不再贴窗口右边框；下拉改玻璃胶囊样式
- 全局磨砂玻璃：标题栏/侧栏/会话头/输入区/菜单走 `backdrop-filter` + `color-mix` 半透明（弹窗类按反馈改回实色）
- 按钮全局圆润玻璃化（btn / btn-sm 胶囊 / btn-danger / btn-ghost / 原生 select、input）；滚动条改半透明胶囊 + hover accent

## 侧栏结构

- 「群聊」头部右侧 ⋮ 菜单收纳「创建群聊 / 加入群聊」，原操作栏删除；房间行的退出按钮删除（退出只留在群聊界面）
- 「文件」栏改抽屉式：收起固定在 footer 上方，点开整栏顶到群聊下方并覆盖会话列表（带 200ms 上滑淡入），再点收回原位；修掉展开时的双线
- 文件/群聊字号与「新会话」统一（1.038rem）；footer「Settings」中文化为「设置」

## 变更栏

- 文件列表与 diff 详情之间加竖向拖拽缝，详情区可上下拉伸（12%–85%）
- 左拉调宽：会话栏 35% 软限（顿一下），二次拉伸才全覆盖（复刻编辑栏覆盖交互）；全覆盖时拖缝向右退出；修复拉到底按钮飞出窗口的 bug（上限未扣侧栏宽）
- 文件行 ↗ 按钮从"打开外部编辑器"改为在内置编辑栏开 tab（项目外路径兜底仍走系统默认）
- diff 展示改为块式：`lineDiff` 对连续改动先集中输出删除行、再输出新增行（不再逐行 -/+ 交替），diff 详情、全文模式、编辑器内联装饰三处同时生效；新增分组单测

## 文件编辑器 & 消息

- **编辑器内联变更**（Cursor/Trae 风）：新模块 `editor-diff-deco.ts`，会话 diff hunk → CodeMirror 装饰；新增行绿底 + 左绿条，删除行红色删除线块插在锚点行上方，只读不影响编辑
- **tab 右键菜单**：固定（📌，批量关闭跳过）/ 关闭右侧 / 关闭其他 / 全部关闭 / 在文件管理器中显示
- **附件气泡**：消息不再显示 `[Attached: …]` 文本，改渲染 chip（图标 + 文件名 + 扩展名角标）；编辑器支持的类型点击进内置编辑栏，其余走系统默认打开；图片 96×96 缩略图（新 IPC `file:image-data`，带缓存），点击全屏 lightbox 预览
- 边界：附件 chip 只对本次运行内的消息生效，重启后旧消息回退为文本尾巴

## 验证

- `pnpm typecheck` ✅（apps/desktop）
- `pnpm test` 349/349 ✅（desktop）；shared 84/84 ✅
- `pnpm build` ✅
- 所有改动未提交，待验收后 commit
