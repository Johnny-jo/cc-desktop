node_modules
dist
out
*.log
.DS_Store
.env
.env.*
coverage
release
*.local
.superpowers
# Prepared binaries for electron-builder (scripts/prepare-vendor.mjs)
vendor/
# Local scratch / download tooling, not project source
tools/
